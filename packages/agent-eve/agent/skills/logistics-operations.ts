import { defineDynamic, defineSkill } from "eve/skills";
import { hasToolboxSkill } from "../lib/capability-profile";
import { businessSemanticCatalogs } from "../lib/business-semantic-catalogs";

const skill = defineSkill({
  description:
    "分析承运商表现、物流异常、包裹轨迹、配送 SLA 和运费。用户询问延迟、丢件、物流时效或履约成本时使用。",
  markdown:
    "## Usage\n\n本项目通过可执行语义层访问 Toolbox。加载本 Skill 后，必须调用 `query_business_data`，传入用户原始问题和本目录中的 canonical candidate；不要直接调用底层业务 Toolbox Tool，也不要生成 SQL。语义 Tool 会完成消歧、时间归一、认证查询契约选择、Toolbox 执行和结果来源封装。\n\n## Workflow\n\n1. 读取 `references/logistics.yaml`，从中选择 catalog、intent、metric 和 dimension canonical id。\n2. 调用 `query_business_data`；不得提交 Tool 名、SQL、表名、列名、租户或授权范围。\n3. 返回 `clarification` 时向用户原样澄清，取得答案后重新解析；不要猜测。\n4. 返回 `unsupported` 时说明当前认证查询目录的限制；不要绕过语义层。\n5. 返回 `result` 时只依据 Semantic result envelope 回答，并说明指标、UTC `[from, to)` 时间窗、维度、过滤和限制。\n\n## Business semantic catalog\n\n涉及业务术语、指标、维度或枚举取值时，只使用该目录认证的术语和口径。底层认证路径为 `get_shipment_trace`、`list_logistics_exceptions`、`summarize_carrier_performance`、`summarize_delivery_sla`、`summarize_freight_costs`，仅供理解覆盖范围，不能由模型直接调用。\n\n## Internal certified query paths\n\n### get_shipment_trace\n\n通过明确的合成运单号返回运单、订单和按时间排序的物流轨迹事件。\n结果最多返回 100 条事件；事件 location 和 detail 均为合成运营信息，不代表实时物流状态。\n\n#### Parameters\n\n| Name           | Type    | Description                                | Required | Default |\n| :------------- | :------ | :----------------------------------------- | :------- | :------ |\n| shipmentNumber | string  | 明确的合成运单号，例如 SHP-20260601-0001。 | Yes      |         |\n| limit          | integer | 最多返回的物流轨迹事件数量。               | No       | `100`   |\n\n---\n\n### list_logistics_exceptions\n\n分页返回明确 UTC 发货时间窗内状态异常、签收超时或截至 to 已超过承诺时间仍未签收的运单。\n结果包含合成订单、仓库、承运商和异常等待小时数，按超时程度和运单 ID 稳定排序。\n\n#### Parameters\n\n| Name   | Type    | Description                                                            | Required | Default |\n| :----- | :------ | :--------------------------------------------------------------------- | :------- | :------ |\n| from   | string  | ISO-8601 UTC 发货时间窗开始时间（包含），例如 2026-06-01T00:00:00Z。   | Yes      |         |\n| to     | string  | ISO-8601 UTC 发货时间窗结束时间（不包含），例如 2026-06-30T00:00:00Z。 | Yes      |         |\n| limit  | integer | 最多返回的异常运单数量。                                               | No       | `50`    |\n| offset | integer | 从稳定排序结果中跳过的异常运单数量；首页传 0。                         | No       | `0`     |\n\n---\n\n### summarize_carrier_performance\n\n按承运商汇总明确 UTC 发货时间窗内的运单量、签收量、准时率、异常量和平均配送时长。\n准时签收定义为 deliveredAt <= promisedAt；未签收运单不计入平均配送时长。\n\n#### Parameters\n\n| Name | Type   | Description                                                            | Required | Default |\n| :--- | :----- | :--------------------------------------------------------------------- | :------- | :------ |\n| from | string | ISO-8601 UTC 发货时间窗开始时间（包含），例如 2026-06-01T00:00:00Z。   | Yes      |         |\n| to   | string | ISO-8601 UTC 发货时间窗结束时间（不包含），例如 2026-06-30T00:00:00Z。 | Yes      |         |\n\n---\n\n### summarize_delivery_sla\n\n按承诺签收日汇总明确 UTC 发货时间窗内的配送 SLA：准时、迟到和截至 to 未签收运单。\nonTimeDeliveryRate 仅以已签收运单为分母；未签收积压单独返回，避免伪造完成时效。\n\n#### Parameters\n\n| Name | Type   | Description                                                            | Required | Default |\n| :--- | :----- | :--------------------------------------------------------------------- | :------- | :------ |\n| from | string | ISO-8601 UTC 发货时间窗开始时间（包含），例如 2026-06-01T00:00:00Z。   | Yes      |         |\n| to   | string | ISO-8601 UTC 发货时间窗结束时间（不包含），例如 2026-06-30T00:00:00Z。 | Yes      |         |\n\n---\n\n### summarize_freight_costs\n\n按承运商汇总明确 UTC 发货时间窗内的运费、平均每单运费、运输距离和每公里成本。\nfreightCost 仅表示合成物流运费，不包含仓储、包装或逆向物流成本，也不能单独代表订单利润。\n\n#### Parameters\n\n| Name | Type   | Description                                                            | Required | Default |\n| :--- | :----- | :--------------------------------------------------------------------- | :------- | :------ |\n| from | string | ISO-8601 UTC 发货时间窗开始时间（包含），例如 2026-06-01T00:00:00Z。   | Yes      |         |\n| to   | string | ISO-8601 UTC 发货时间窗结束时间（不包含），例如 2026-06-30T00:00:00Z。 | Yes      |         |\n\n---\n",
  files: {
    "references/logistics.yaml": businessSemanticCatalogs["logistics.yaml"],
  },
});

export default defineDynamic({
  events: {
    "session.started": () =>
      hasToolboxSkill("logistics-operations") ? skill : null,
  },
});
