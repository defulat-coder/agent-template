# Compatibility alias: bounded Agent streams and local build load

This historical path is retained because committed ZRead snapshots cite source
paths as durable links. The accepted decision was renumbered without changing
its content; read
[ADR 0017](./0017-bound-agent-stream-memory-and-local-build-load.md) for the
canonical record.
